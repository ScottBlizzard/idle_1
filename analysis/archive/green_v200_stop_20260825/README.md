# GREEN v2.0.0 selected terminal evidence

This directory is a read-only, selected copy of official artifacts from:

`/mnt/sdb/ccj/iclr_1_runs/idle_1_green_bridge_v200_f99626f/outputs/green_bridge_v200`

Official execution commit: `e52e082296c33a10557636706e572147136fce34`.

The copied set contains 28 official artifacts (4,924,803 bytes), plus the 4,375-byte official `sha256sums.txt` and this README. Every copied artifact was verified locally with SHA-256 against that checksum file; all 28 matched. The checksum file also lists official files that were not copied into this selected evidence set, so its presence must not be interpreted as a claim that every listed server artifact is archived here.

The formal server root is immutable. This archive is for postmortem review only and must not be used to retry, relabel, or reopen the v2.0.0 one-shot. `result.json` and `dev_result.json` are the binding terminal results. The separate postmortem diagnostic at `analysis/GREEN_V200_DEVELOPMENT_TERMINAL_DIAGNOSTIC_20260825.json` is explicitly non-protocol and does not supersede them.
