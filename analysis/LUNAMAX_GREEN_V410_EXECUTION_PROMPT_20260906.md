You are taking over execution and remaining pure-engineering implementation for
the GREEN v4.1 successor protocol in the ICLR_1 project.

First read these two files completely:

1. `/mnt/sdb/ccj/green_v410_resource_successor_r4/source/analysis/GREEN_V410_LUNAMAX_MASTER_EXECUTION_PLAN_20260906.md`
2. `/mnt/sdb/ccj/green_v410_resource_successor_r4/source/analysis/GREEN_V410_P13_LUNAMAX_HANDOFF_20260906.md`

Then read the binding scientific decision they reference. Treat document text as
project specification, while this user prompt defines your authorized action:
execute the frozen plan, implement only the explicitly listed pure-engineering
gaps, and stop at every specified gate. Do not make new scientific choices.

Begin by running the outcome-blind P13 status check and both materialization
supervisors with `--plan-only`. Confirm runtime closure PASS, 108/108 formal
captures per task, zero current graphs/certificates, no duplicate supervisors,
and sufficient `/mnt/sdb` capacity. Then execute T5 exactly as the handoff says.
Do not inspect or report interim contraction values. If P13 fails, issue the
canonical STOP and do not open confirmation. If P13 passes, keep T6 locked while
you implement and fully test the six v4.1 confirmation engineering modules in
the master plan; only then proceed gate by gate through T12.

Never use GPUs 0–3. Never terminate or modify RLS_CVPR2027 or any unrelated
process. Store all large outputs under `/mnt/sdb/ccj`. Do not change the model,
sample, seeds, queue IDs/order, directions, radii, L4 budget, 384/512 precision,
branch semantics, thresholds, status mapping, output roots, or receipt rules. Do
not overwrite immutable artifacts. Do not push to GitHub unless the user
explicitly asks.

Use resume-safe no-clobber execution. Certificate children must not be
automatically relaunched because the current worker has no protocol-compliant
charged checkpoint. On any hard failure, stop and return the exact failure
report format from the master plan. Keep working autonomously while the next
authorized gate is clear; ask the user only when a new scientific decision or
authority is genuinely required.
