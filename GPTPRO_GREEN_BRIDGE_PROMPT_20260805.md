# Prompt to submit to GPT Pro: real-transformer green bridge

Act as the lead theoretical researcher and mechanistic-intervention designer for
the repository:

**https://github.com/ScottBlizzard/idle_1** (branch: `main`)

Read, in order:

1. `GPTPRO_0805.md`;
2. `analysis/GPTPRO_THEORY_PACKAGE_20260805.md`;
3. `analysis/CPU_THEORY_GATE_EXECUTION_20260805.md`;
4. `src/mixed_path_identification.py`;
5. `src/test_mixed_path_identification.py`;
6. `src/run_mixed_path_synthetic.py`.

The execution agent has completed and passed the full CPU analytic contract.
Do not repeat the ASG-RDAG derivation and do not request more generic synthetic
tests. The only unresolved issue is the **real-transformer structural bridge**
that prevented THEORY GREEN.

## Binding objective

Decide whether one topology-correct, computationally feasible GPT-2-small
Greater-Than experiment can instantiate a non-tautological positive
identification theorem whose target is an independently meaningful absolute
path-specific effect on the Greater-Than logit margin.

You must either:

1. provide a complete theorem-plus-experiment bridge that licenses one frozen
   GPU experiment; or
2. prove/explain why no defensible bridge is available under the remaining
   budget and issue **BRIDGE RED**.

Do not return AMBER without one sharply stated missing lemma and a concrete
reason it cannot be resolved from the known architecture.

## Required architecture audit

Audit the exact GPT-2-small computation order and TransformerLens hook semantics.
The previous grouping is prohibited because head 9.1 is interleaved with MLPs
8--11 and raw MLP residual outputs are not separable gate inputs.

Determine whether a valid experiment can be built using exactly one of the
following kinds of solutions, or a rigorously superior alternative:

- topology-correct sequential subgraphs rather than one invalid two-block cut;
- actual MLP preactivation/gate coordinates with known GELU derivatives and
  observed anchors;
- edge-specific or path-specific interventions that observe and subtract bypass
  terms rather than inferring a path decomposition from the total Hessian;
- an architecture-derived path-Jacobian product whose inverse is proved for the
  selected transformer subgraph.

These are possibilities to audit, not assumptions that one must work.

## Required positive deliverable if GREEN

If a valid bridge exists, provide all of the following.

### Exact computational DAG

- Every selected node and edge in forward topological order.
- Exact layer, component, token position, and hook location.
- Exact distinction among attention result, residual write, MLP input,
  preactivation, post-GELU activation, MLP output, residual stream, LayerNorm,
  and logits.
- A proof that every declared upstream mediator precedes every downstream
  mediator used in a mixed estimator.

### Structural equations and inverse

- Structural equations for the selected real-transformer subgraph.
- The independently defined path parameter or equivalence class.
- Exact observable interventions and response tensors.
- A derived injective map from those observables to the path target.
- Explicit treatment of residual, LayerNorm, attention, serial-MLP, and omitted
  bypass terms.
- Constructive failure cases when any new assumption is removed.

Do not assume a complete cut, positive inverse constant, faithfulness, or bypass
absence without deriving it from the chosen hook/intervention design or making
it directly testable by edge isolation.

### Independent absolute target

- Define the path-specific effect in logit-margin units.
- Explain how it is measured independently of the proposed predictor.
- Prove the relation between the target and the identified structural object.
- Do not use attention probability, activation cosine, behavioral restoration,
  or a near-zero-denominator recovery ratio.

### Feasible probe design

- Declare the exact fixed basis or coordinate selector.
- Preserve actual gate coordinates wherever curvature correction is used.
- Give dimensions `r1`, `r2`, and output dimension `k`.
- Give the exact number of first-order and four-corner forward passes per item.
- Prove the finite design has the required marginal and Kronecker ranks.
- Explain why the selected subspace is scientifically meaningful without
  claiming coverage outside it.

### Exact execution manifest

- Model and task prompts.
- Frozen semantic cells and split sizes.
- Corruption rule.
- Donor/basis, development, tensor-fit, energy-evaluation, and confirmation
  splits.
- Exact hooks and intervention order.
- Radius and half-radius rules.
- Structural admissibility and curvature thresholds.
- Fair single-direction, equally budgeted first-order, PIE/INT-style, and
  behavioral baselines.
- Cell-level estimands and paired cell-bootstrap analysis.
- Decisive success, failure, and early-stop conditions.
- Estimated GPU memory, forward-pass count, and wall-clock budget for a 4090 and
  an A40-class GPU.

The execution description must be precise enough that the agent can implement
it without inventing theory, hooks, targets, sites, thresholds, or statistical
choices.

## Required RED deliverable if no bridge exists

If no valid bridge exists, identify the minimal impossibility obstruction. State
whether the failure comes from topology, non-injective path allocation,
LayerNorm/downstream nonlinearity, infeasible probe completeness, lack of an
independent target, or their combination.

Then state whether the project should:

- stop the oral bid and retain the ASG-RDAG theorem as a poster-level boundary
  result; or
- abandon the current pretrained-model requirement in favor of a different
  theorem class.

Do not recommend exploratory GPU work after BRIDGE RED.

## Prohibited shortcuts

Do not:

1. reuse the rejected raw head-output/MLP-output grouping;
2. call a total mixed Hessian a path tensor without isolating bypass terms;
3. use PCA rotations on gate coordinates while retaining coordinatewise GELU
   curvature claims;
4. use ridge regression to conceal rank deficiency;
5. let development or confirmatory outcomes select hooks, heads, neurons,
   subspaces, corruption, or radius;
6. use the same path-patching measurement as both predictor and target;
7. replace a proof with more seeds or larger models;
8. claim complete-circuit identification from a selected low-dimensional
   subspace;
9. authorize a server run whose scientific interpretation remains ambiguous.

## Output contract

Return one self-contained Markdown document intended to be saved verbatim as:

`analysis/GPTPRO_GREEN_BRIDGE_20260805.md`

Use exactly these top-level sections:

1. `# Bridge Verdict`
2. `# Exact Transformer DAG Audit`
3. `# Structural Object and Identification Result`
4. `# Complete Proof or Impossibility Argument`
5. `# Independent Path-Specific Target`
6. `# Probe-Complete Finite Design`
7. `# Frozen Greater-Than Experiment`
8. `# TransformerLens Hook Contract`
9. `# Statistical Analysis and Decision Thresholds`
10. `# Compute and Early-Stop Budget`
11. `# Claim Boundary`
12. `# Binding Execution Checklist`

End with exactly one of:

- **BRIDGE GREEN — SERVER EXECUTION AUTHORIZED**, followed by a complete frozen
  manifest; or
- **BRIDGE RED — DO NOT OPEN THE SERVER**, followed by the terminal project
  decision.

Do not write conversational material outside the Markdown document.
